const HealthStrategy = require('../strategy')

class CopaceticStrategy extends HealthStrategy {
 //TODO something that expects the adapter and sends the message expecting to receive copacetic health data as a response
}

module.exports = CopaceticStrategy
